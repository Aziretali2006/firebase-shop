
const mainPath = {
  layout: "/*",
  auth: "accounts/*"
};

const AuthPath = {
  register: "/signup",
  login: "/login"
}

const connectedAuthPath = {
  register: "/accounts/signup",
  login: "/accounts/login"
}

export const PATH = {
  mainPath,
  AuthPath,
  connectedAuthPath
}